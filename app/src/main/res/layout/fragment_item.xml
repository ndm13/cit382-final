<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:app="http://schemas.android.com/apk/res-auto"
              android:layout_width="match_parent"
              android:layout_height="wrap_content"
              android:orientation="horizontal">
    <!--
        We have a lot of elements that have implied interaction.  That interaction should be
        implemented by linking this layout to MainActivity and using android:onClick XML attributes
        on the clickable items.
         - TODO imageContainer: onClick should open an image viewing intent on GeoPhoto.fileName
         - TODO mainText: onClick should switch to the map fragment with focus on the image marker
         - TODO shareButton: onClick should open an image sharing intent on GeoPhoto.fileName
         - TODO deleteButton: onClick should remove the entry from the DB and delete the file
    -->

    <ImageButton
        android:id="@+id/imageContainer"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_weight="1"
        app:srcCompat="@android:drawable/ic_menu_camera"/>

    <LinearLayout
        android:id="@+id/mainText"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/fileName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="File Name"
            android:textSize="18sp"/>

        <TextView
            android:id="@+id/fileInfo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Coordinates - DateTime"/>
    </LinearLayout>

    <ImageButton
        android:id="@+id/shareButton"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_weight="1"
        app:srcCompat="@android:drawable/ic_menu_share"/>

    <ImageButton
        android:id="@+id/deleteButton"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_weight="1"
        app:srcCompat="@android:drawable/ic_menu_delete"/>

</LinearLayout>
